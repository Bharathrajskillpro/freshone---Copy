import 'package:flutter/material.dart';

class Collab extends StatelessWidget {
  const Collab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Will be available on next Version!'),
    );
  }
}
